/**
 * The delay between each interval tick. Used on iOS where some
 * methods don't return the result, but the result is given
 * outside of SpotifyAppRemote class.
 */
export const INTERVAL_DELAY = 1000;

/**
 * The limit of the interval execution.
 */
export const INTERVAL_LIMIT = 30000;
